export enum ComponentIs {
    BUTTON = 'button',
    ANCHOR = 'anchor',
}

export enum Types {
    PRIMARY = 'primary',
    SECONDARY = 'secondary',
}

export enum Sizes {
    SMALL = 'small',
    DEFAULT = 'default',
}